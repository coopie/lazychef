language: python
python:
  - "3.5"
  # these guys have trouble installing h5py
  # - "3.6"
  # - "nightly" # currently points to 3.7-dev
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: py.test test
